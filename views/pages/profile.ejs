<%- include('../partials/header') %>
<%- include('../partials/menu') %>

<div class="container">
    <table class="table">
        <tr>
            <th>UserName</th>
            <td><%= username %></td>
        </tr>
        <tr>
            <th>Email</th>
            <td><%= email %></td>
        </tr>
        <tr>
            <th>Admin?</th>
            <% if(isadmin){ %>
            <td>Yes</td>
            <% } else { %>
            <td>No</td>
            <% } %>

        </tr>
    </table>
</div>

<%- include('../partials/footer') %>
